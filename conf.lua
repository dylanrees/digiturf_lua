function love.conf(t)

    -- to change the number of blocks the screen spans, change the variables directly below
	xblocks = 50
	yblocks = 40

    t.window.width = xblocks*16
    t.window.height = yblocks*16
    
end
